{
  "$schema": "http://schema.management.azure.com/schemas/2015-01-01/deploymentTemplate.json#",
  "contentVersion": "1.0.0.0",
  "parameters": {
    "nicName": {
      "type": "string",
      "metadata": {"description": "Name of Network Interface"}
    },
    "subnetRef": {
      "type": "string",
      "defaultValue": "SubnetDefault",
      "metadata": {"description": "A reference to an existing subnet"}
    },
    "location": {
      "type": "string",
      "metadata": {"description": "A reference to a resource location"
      }
    },
    "staticIpAddress": {
      "type": "string",
      "metadata": {
        "description": "Static IP Address for Network Interface"
      } 
    }
  },
  "variables": {},
  "resources": [
    {
      "apiVersion": "2015-05-01-preview",
      "type": "Microsoft.Network/networkInterfaces",
      "name": "[parameters('nicName')]",
      "location": "[parameters('location')]",
      "dependsOn": [],
      "properties": {
        "ipConfigurations": [
          {
            "name": "ipconfig1",
            "properties": {
              "privateIPAllocationMethod": "Static",
              "privateIPAddress": "[parameters('staticIpAddress')]",
              "subnet": {
                "id": "[parameters('subnetRef')]"
              }
            }
          }
        ]
      }
    }
  ]
}
